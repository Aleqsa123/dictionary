import styled from 'styled-components';

const Header = styled.h1`
  font-size: 1.5em;
  text-align: center;
  color: blue;
  margin: 15px;
`

const Title = () => {
    return ( <Header>Georgian-Ossetian Dictionary</Header> )
}

export default Title;
